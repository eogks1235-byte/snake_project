{
  "permissions": {
    "allow": [
      "Bash(python -c ' *)",
      "Bash(python -c \"import torch; print\\(f'torch {torch.__version__}'\\); print\\(f'cuda: {torch.cuda.is_available\\(\\)}'\\)\")"
    ]
  }
}
